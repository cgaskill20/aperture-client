import geojsonvt from 'geojson-vt';
import xyz from 'xyz-affair';

export default function newRendering(geojson) {
    //const tiles = xyz(globalThis.map.getBounds(),)
    var tileIndex = geojsonvt(geojson);
}